import { ClientPage } from "@/features/dashboard/client-page";

export const metadata = {
  title: "FaithTools - Tableau de Bord",
};

export default async function Dashboard() {
  return <ClientPage />;
}
